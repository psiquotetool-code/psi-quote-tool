# Start Development

Start the Next.js development environment:

1. Check if `node_modules` exists, if not run `npm install`
2. Check if `.env.local` exists, warn if missing
3. Run `npm run dev` to start the Next.js dev server
4. Report that the app is available at http://localhost:3000
5. Watch for any startup errors and report them
